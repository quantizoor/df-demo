{
  "mcpServers": {
    "evidence": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/server.js",
        "--released-evidence-root",
        "${DF_RELEASED_EVIDENCE_ROOT}",
        "--submission-root",
        "${DF_OPTIMIZER_SUBMISSION_ROOT}",
        "--audit-root",
        "${DF_OPTIMIZER_AUDIT_ROOT}",
        "--campaign-id",
        "${DF_CAMPAIGN_ID}",
        "--project-root",
        "${CLAUDE_PROJECT_DIR}",
        "--plugin-data",
        "${DF_PLUGIN_DATA_ROOT}"
      ]
    }
  }
}
