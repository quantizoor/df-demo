# Default-deny the OCI build context. Images receive only reviewed source,
# dependency metadata, and build inputs; Git history, tests, docs, local state,
# and secrets never enter BuildKit.
**

!package.json
!pnpm-lock.yaml
!pnpm-workspace.yaml
!tsconfig.json
!tsconfig.build.json
!vitest.config.ts
!biome.json

!src/
!src/**
!scripts/
!scripts/**
!claude-plugin/
!claude-plugin/**
!containers/
!containers/**
